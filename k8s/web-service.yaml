apiVersion: v1
kind: Service
metadata:
  name: cnas-web
  labels:
    app.kubernetes.io/name: cnas-web
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cnas-web
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: http

